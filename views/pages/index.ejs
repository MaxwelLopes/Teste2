<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bem-Vindo!</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100vw;
            min-height: 100vh;
        }

        main {
            background-color: #fff;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 600px;
            color: #333;
            position: relative; 
            flex: 1;
        }

        h1 {
            color: #333;
            margin-bottom: 20px;
        }

        p {
            margin-bottom: 30px;
            color: #555;
        }

        .logout {
            display: inline-block;
            padding: 10px 20px;
            margin-top: 20px;
            text-decoration: none;
            color: #4caf50;
            font-weight: bold;
            border: 2px solid #4caf50;
            border-radius: 6px;
            transition: background-color 0.3s ease-in-out, color 0.3s ease-in-out;
        }

        .logout:hover {
            background-color: #45a049;
            color: #fff;
        }

        form {
            margin-top: 30px;
        }

        label {
            display: block;
            margin-bottom: 10px;
            color: #333;
        }

        textarea {
            width: 100%;
            padding: 10px;
            box-sizing: border-box;
            margin-bottom: 20px;
            border: 2px solid #4caf50;
            border-radius: 6px;
            background-color: #f4f4f4;
            color: #333;
            resize: none;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            border: none;
            padding: 12px 24px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 18px;
            cursor: pointer;
            border-radius: 8px;
            transition: background-color 0.3s ease-in-out;
        }

        button:hover {
            background-color: #45a049;
        }

        h2 {
            color: #333;
            margin-top: 30px;
        }

        .post {
            border-top: 1px solid #ddd;
            padding-top: 20px;
            margin-top: 20px;
            text-align: left;
            white-space: pre-line;
        }

        strong {
            color: #333;
        }

        svg {
            width: 50px;
            height: 50px;
            fill: #4caf50;
            margin-bottom: 10px;
        }

        .back-to-top {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background-color: #4caf50;
            color: #fff;
            border: none;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: none;
            font-size: 16px;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.3s ease-in-out;
        }

        .back-to-top:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>

<main>
    <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" stroke="#45a050"><g id="SVGRepo_bgCarrier" stroke-width="0"></g><g id="SVGRepo_tracerCarrier" stroke-linecap="round" stroke-linejoin="round"></g><g id="SVGRepo_iconCarrier"> <path d="M16 7C16 9.20914 14.2091 11 12 11C9.79086 11 8 9.20914 8 7C8 4.79086 9.79086 3 12 3C14.2091 3 16 4.79086 16 7Z" stroke="#4caf50" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path> <path d="M12 14C8.13401 14 5 17.134 5 21H19C19 17.134 15.866 14 12 14Z" stroke="#4caf50" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path> </g></svg>

    <h1>Bem-vindo, <%= user.name %>!</h1>
    <p>Email: <%= user.email %></p>
    <a href="/logout" class="logout">Sair</a>

    <form action="/create-post" method="post">
        <label for="postContent">Faça um novo post:</label>
        <textarea id="postContent" name="postContent" rows="4" required></textarea>
        <br>
        <button type="submit">Postar</button>
    </form>

    <div>
        <h2>Posts</h2>
        <% posts.forEach(post => { %>
            <div class="post">
                <p><strong><%= post.author %></strong> <br> <%= post.content %></p>
            </div>
        <% }); %>
    </div>

    <!-- Botão de volta ao topo -->
    <button class="back-to-top" onclick="scrollToTop()">Voltar ao Topo</button>
</main>

<script>
    // Função para rolar até o topo da página
    function scrollToTop() {
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    }

    // Exibir ou ocultar o botão de volta ao topo com base no scroll da página
    window.onscroll = function () {
        var button = document.querySelector('.back-to-top');
        if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
            button.style.display = "block";
        } else {
            button.style.display = "none";
        }
    };
</script>

</body>
</html>
